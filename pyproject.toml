[project]
name = "adm-gate-cam"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
	"PyQt6>=6.6",
	"paho-mqtt>=2.0",
]

[tool.uv.workspace]
members = [
    "web/backend",
]
